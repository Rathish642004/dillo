# Card

A neutral surface container. `variant="navy"` for dark-band callouts;
`interactive` adds the signature red underline on hover plus a -2px lift.

```jsx
<Card pad="lg" interactive>
  <h3>Healthcare uniforms</h3>
  <p>Scrubs, lab coats, ward shirts.</p>
</Card>
```
