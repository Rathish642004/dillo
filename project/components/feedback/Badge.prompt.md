# Badge

Small status / category tag. Pass `banner` to render the signature
squared-off red uppercase tag (looks like a tiny version of the DILLO
banner) — used for "BESTSELLER" or "MADE TO ORDER" labels on product cards.

```jsx
<Badge variant="red" banner>Bestseller</Badge>
<Badge variant="success">In stock</Badge>
```
