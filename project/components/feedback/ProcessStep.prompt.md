# ProcessStep

A single step in the "How It Works" rail. Big red two-digit numeral on top,
optional navy icon, then title + description.

```jsx
<ProcessStep number="01" title="Tell us what you need" description="Pick a category, share quantity, target date." icon={<ClipboardList size={20}/>} />
```
