# Footer

Dark navy footer with a 4px red rule running across the top edge, four
columns (brand + tagline / products / company / reach us), and a thin
copyright row.

```jsx
<Footer onNavigate={(k) => setPage(k)} />
```
