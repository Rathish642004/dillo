# WhatsAppFab

Fixed bottom-right floating action button that opens a `wa.me` deep link.
Required across every public page (per the brief) — mount once at the root.

```jsx
<WhatsAppFab phone="919820212345" message="Hi Dillo, I'd like a uniform quote." />
```
